- name: Apache Deployment on EC2
  hosts: dev
  become: true
  gather_facts: true

  tasks:
    - name: Install latest Apache
      ansible.builtin.yum:
        name: httpd
        state: latest

    - name: Ensure Apache is running and enabled
      ansible.builtin.service:
        name: httpd
        state: started
        enabled: true