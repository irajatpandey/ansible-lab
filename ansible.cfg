[defaults]
inventory         = ./inventory/hosts.ini
remote_user       = ec2-user
private_key_file  = ~/.ssh/rajat.pem   # ← change to your .pem filename
host_key_checking   = False
retry_files_enabled = False
stdout_callback     = default
result_format       = yaml

[privilege_escalation]
become        = True
become_method = sudo
